# ═══════════════════════════════════════════════════════════════
# SimpleClaw v3.0 - Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copie para .env e preencha os valores obrigatórios.
# Linhas com CHANGE_ME precisam ser editadas.

# ── ENGINE ────────────────────────────────────────────────────
# "loop" = agent loop direto (NOVO, recomendado)
# "agno" = framework Agno (legado/fallback)
SIMPLECLAW_ENGINE=loop

# ── IDENTITY ──────────────────────────────────────────────────
SIMPLECLAW_APP_NAME=SimpleClaw
SIMPLECLAW_APP_VERSION=3.0.0
SIMPLECLAW_DEBUG=false
SIMPLECLAW_LOG_LEVEL=INFO

# ── TELEGRAM ──────────────────────────────────────────────────
SIMPLECLAW_TELEGRAM_TOKEN=CHANGE_ME
SIMPLECLAW_TELEGRAM_ADMIN_IDS=[CHANGE_ME]

# ── LLM - ROUTER (modelo principal) ──────────────────────────
# Provider: groq, openai, ollama, anthropic, litellm
SIMPLECLAW_ROUTER_PROVIDER=groq
SIMPLECLAW_ROUTER_MODEL_ID=llama-3.1-8b-instant
SIMPLECLAW_ROUTER_API_KEY=CHANGE_ME
SIMPLECLAW_ROUTER_API_BASE=https://api.groq.com/openai/v1
SIMPLECLAW_ROUTER_TEMPERATURE=0.3
SIMPLECLAW_ROUTER_MAX_TOKENS=4096

# ── LLM - SPECIALIST (legado Agno, ignorado no engine=loop) ──
SIMPLECLAW_SPECIALIST_PROVIDER=groq
SIMPLECLAW_SPECIALIST_MODEL_ID=llama-3.1-8b-instant
SIMPLECLAW_SPECIALIST_API_KEY=CHANGE_ME
SIMPLECLAW_SPECIALIST_API_BASE=https://api.groq.com/openai/v1
SIMPLECLAW_SPECIALIST_TEMPERATURE=0.2
SIMPLECLAW_SPECIALIST_MAX_TOKENS=8192

# ── DATABASE (PostgreSQL) ────────────────────────────────────
# Formato: postgresql+psycopg://user:pass@host:port/dbname
# Dentro do docker compose: usar nome do serviço (postgres)
SIMPLECLAW_DATABASE_URL=postgresql+psycopg://simpleclaw:simpleclaw@postgres:5432/simpleclaw

# ── REDIS ─────────────────────────────────────────────────────
# Dentro do docker compose: usar nome do serviço (redis)
SIMPLECLAW_REDIS_URL=redis://redis:6379/0

# ── SESSIONS ──────────────────────────────────────────────────
SIMPLECLAW_SESSIONS_DIR=/var/simpleclaw/sessions

# ── SEARCH (SearXNG) ─────────────────────────────────────────
# Dentro do docker compose: usar nome do serviço (searxng)
SIMPLECLAW_SEARXNG_URL=http://searxng:8080
SIMPLECLAW_SEARXNG_TIMEOUT_SECONDS=15

# ── SUPERSET (Dashboards) ────────────────────────────────────
SIMPLECLAW_SUPERSET_URL=http://superset:8088
SIMPLECLAW_SUPERSET_USERNAME=admin
SIMPLECLAW_SUPERSET_PASSWORD=admin

# ── VAULT (Encryption) ───────────────────────────────────────
# Gerar com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SIMPLECLAW_VAULT_MASTER_KEY=CHANGE_ME

# ── AUDIO ─────────────────────────────────────────────────────
# Provider: groq (usa Whisper via Groq API) ou openai
SIMPLECLAW_WHISPER_PROVIDER=groq
SIMPLECLAW_WHISPER_API_KEY=
SIMPLECLAW_TTS_ENABLED=false
SIMPLECLAW_TTS_VOICE=pt_BR-faber-medium

# ── COST TRACKING ─────────────────────────────────────────────
SIMPLECLAW_ENABLE_COST_TRACKING=true
SIMPLECLAW_COST_ALERT_THRESHOLD_USD=10.0

# ── ISOLATION ─────────────────────────────────────────────────
SIMPLECLAW_WORKER_BASE_DIR=/tmp/simpleclaw_workers
SIMPLECLAW_WORKER_TIMEOUT_AFTER_TASK_MINUTES=25
SIMPLECLAW_WORKER_MAX_CONCURRENT=2

# ── BACKUP ────────────────────────────────────────────────────
SIMPLECLAW_BACKUP_CRON=0 3 * * *
SIMPLECLAW_BACKUP_RETENTION_DAYS=30
